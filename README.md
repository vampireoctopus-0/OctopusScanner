# OctopusScanner
# VampireScan v1.0 A lightweight, multi-threaded Python Port Scanner and Banner Grabber designed for quick network reconnaissance during penetration testing engagements.  Features.Built using Python's native socket library. Implements timeouts to prevent hang-ups on filtered ports.  Runs entirely on standard Python libraries (No Installation req). 
